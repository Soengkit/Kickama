openapi: 3.1.0
info:
  version: 1.0.0
  title: Ordering Fixture
paths:
  /widgets:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                properties:
                  name:
                    type: string
                  id:
                    type: string
                required:
                  - name
                  - id
                type: object
          description: OK
      tags:
        - Alpha
        - Beta

