**Bounty:** $25 (LT)

Acceptance criteria:
- Validate required sections before publishing bounty issues

Commissions:

You can earn an extra $5 for every bounty issue you create on your own fork of the repo, but this paragraph has been changed.

Required validation:
- Run `python3 build.py`
- Include the generated diagnostic `.logd` artifact from `diagnostic/build-XXX.logd` in the PR notes; this must be a real diagnostic log and must not be `diagnostic/build-00000000.logd`
